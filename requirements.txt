streamlit==1.26.0  # Go slightly older for Streamlit to increase compatibility with Python 3.8 ecosystem. 1.37.0 might still pull newer deps.
tensorflow==2.3.0
h5py==2.10.0      # Compatible with TF 2.3.0 and Python 3.8
numpy==1.19.5     # Compatible with TF 2.3.0 and Python 3.8, and should build cleanly.
matplotlib==3.3.4 # Older matplotlib for Python 3.8 stability
pydub
scipy==1.5.4      # Older scipy for Python 3.8 stability
Pillow==8.4.0     # Older Pillow for Python 3.8 stability

# streamlit==1.37.0
# tensorflow==2.8.0 # THIS IS THE NEW CHANGE!
# h5py==3.1.0       # Keep this, it's generally compatible
# numpy==1.20.0     # Let's try this specific version, as it satisfies Streamlit's >=1.20 and is the lower bound.
# matplotlib
# pydub
# scipy
# Pillow==9.5.0
# protobuf==3.20.3
